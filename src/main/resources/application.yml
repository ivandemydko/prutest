spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/pruDB?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Europe/Helsinki
        username: root
        password: root
mybatis:
     mapper-locations: classpath*:/mybatis/*Mapper.xml